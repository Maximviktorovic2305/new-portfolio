export interface Skill {
  name: string;
  category: string;
  emoji: string;
  description: string;
  website: `https://${string}`;
}

export interface SkillCategory {
  label: string;
  emoji: string;
  color: string;
}
